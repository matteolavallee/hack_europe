// Placeholder: Typescript interfaces mapping directly to the Pydantic schemas in the Backend.
// Ensures strict type-checks between the python data layer and the react view layer.

export interface PatientContext { }
export interface Reminder { }
export interface Caregiver { }
